# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FlightInfo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FlightInfo
  pod 'VerticalAlignmentLabel'
  pod 'IBAnimatable'
  pod 'SwiftSoup'
  pod 'Kanna', '~> 4.0.0'
  
  target 'FlightInfoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FlightInfoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
